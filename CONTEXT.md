# 场景九术语表

## 本地联动 Agent POC
在本地浏览器和本地 Node 服务之间形成可操作闭环的验证体。驾驶舱负责展示和交互，本地服务负责调用 `local-agent/agent.mjs` 读取样例数据、运行合同解析、A 监督、B 结算、C 付款校验与 GLM 研判模拟，并返回结构化结果。

## 受控型履约结算 Agent
面向采购履约追踪与结算闭环的 Agent 方案形态。它可以完成合同解析、履约监督、结算汇算、付款核验、异常分支和台账输出，但付款放行、扣罚调整、转人工等高风险动作必须保留人工确认或授权。

## HITL 人工确认
Human-in-the-loop。用于表达财务/采购员在付款放行、扣罚调整、异常处置中的责任边界。它不是临时补丁，而是业财一体化场景的合规与安全边界。

## 目标行为流程
合同解析节点从长文本合同条款中抽取履约节点、时间要求、数量质量、付款条件后生成的可执行流程：发货 → 到货 → 入库 → 质检 → 结算 → 付款。后续 A 监督节点以此为基准比对实际执行情况。

## 三方一致性校验
C 付款校验节点的核心机制。将合同金额、采购订单金额、发票金额三方比对：一致 / 不一致 / 缺依据 / 需人工复核。用于防范错付漏付的合规风险。

## 样例数据
用于本地 POC 验证的结构化履约订单输入，包括合同条款、物流轨迹、入库质检、发票付款、供应商档案、结算规则。样例数据不等同于真实产线实时数据流。

## 真实产线接入
指后续接入真实 ERP、业财一体化、物流 GPS、采购合同库和生产控制接口。当前本地联动 Agent POC 尚未完成真实产线接入。

## rule-fallback 降级
当 GLM_API_KEY 未配置或调用失败时，GLM 研判节点降级到规则研判。结果会明确标注 `decisionEngine=rule-fallback`，不伪装成 LLM 输出。这是可追溯性的要求，不是缺陷隐藏。
